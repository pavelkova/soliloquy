type Setting {
  id: ID!
  user: User!
  key: String!
  value: String
}

type Query {
  findSettingByName(key: String!): Setting
  findAllSettings: [Setting]
}

type Mutation {
  createSetting(key: String!, value: String): Setting
  updateSetting(id: ID!, key: String!, value: String): Setting
}
