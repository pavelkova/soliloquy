#import "../fragments/ActivityLogFragment.graphql"
#import "../fragments/EntryFragment.graphql"
#import "../fragments/UserFragment.graphql"

mutation ($date: String!, $timezone: String!, $content: String!, $wordCount: Int!, $lowestWordCount: Int!, $startTime: DateTime!) {
  createEntry(date: $date, timezone: $timezone, content: $content, wordCount: $wordCount, lowestWordCount: $lowestWordCount, startTime: $startTime) {
    ...EntryFragment
    user {
      ...UserFragment
    }
    activityLogs {
      ...ActivityLogFragment
    }
  }
}
