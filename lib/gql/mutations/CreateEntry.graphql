#import "../ActivityLogFragment.graphql"
#import "../EntryFragment.graphql"
#import "../UserFragment.graphql"

mutation ($date: String!) {
  findOrCreateEntry (date: $date) {
    ...EntryFragment
    user {
      ...UserFragment
    }
    activityLogs {
      ...ActivityLogFragment
    }
  }
}
