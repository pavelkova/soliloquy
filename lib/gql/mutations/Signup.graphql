#import "../UserFragment.graphql"

mutation ($email: String!, $password: String!, $browserTimezone: String!) {
  signup(email: $email, password: $password, browserTimezone: $browserTimezone) {
    ...UserFragment
  }
}
